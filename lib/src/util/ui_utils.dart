import 'package:flutter/material.dart';

// Define a constant variable called `smallVerticalSpacer` of type `SizedBox`.
// This variable represents a small vertical spacer with a height of 12 pixels.
const smallVerticalSpacer = SizedBox(
  height: 12,
);

// Define a constant variable called `verySmallVerticalSpacer` of type `SizedBox`.
// This variable represents a very small vertical spacer with a height of 8 pixels.
const verySmallVerticalSpacer = SizedBox(
  height: 8,
);
